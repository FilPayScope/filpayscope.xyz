export const siteConfig = {
  name: "FilPayScope",
  url: "https://www.filpayscope.xyz",
  description:
    "Filecoin Pay Analytics Layer. Monitor rail health, forecast funding depletion, and integrate payment analytics to scale with confidence, not risk.",
  baseLinks: {
    home: "/",
  },
};

export type SiteConfigType = typeof siteConfig;
